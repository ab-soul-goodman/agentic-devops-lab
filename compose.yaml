services:
  app:
    build:
      context: .
    init: true
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3000
    ports:
      - "${APP_PORT:-3000}:3000"
    volumes:
      - app-data:/app/data

volumes:
  app-data:
